import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

/*
 * Porest Aspect Ratio (shadcn 베이스 + Porest 디자인 토큰)
 *
 * - Radix AspectRatioPrimitive 그대로 export (스타일 0)
 * - 사용: <AspectRatio ratio={16 / 9}><img ... /></AspectRatio>
 */

const AspectRatio = AspectRatioPrimitive.Root;

export { AspectRatio };
