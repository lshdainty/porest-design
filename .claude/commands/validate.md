# Validate Design System

다음을 순서대로 실행:
1. `npm run lint` — 스펙 준수 검증
2. `npm run diff` — 직전 스냅샷 대비 변경사항 출력
3. WCAG 대비비 위반 토큰 모두 리스트업
4. 미사용 토큰 (어떤 컴포넌트에도 참조 안 됨) 식별

문제 없으면 ✅, 있으면 수정안 제안하되 자동 수정은 하지 마세요.
